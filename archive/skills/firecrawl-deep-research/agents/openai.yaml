interface:
  display_name: "Firecrawl Deep Research"
  short_description: "Run multi-source deep research with Firecrawl. Use when the user asks to resear..."
  default_prompt: "Use $firecrawl-deep-research when the task clearly matches this skill's documented trigger."
policy:
  allow_implicit_invocation: false
