interface:
  display_name: "Firecrawl"
  short_description: "Use Firecrawl CLI for web extraction"
  default_prompt: "Use $firecrawl to search, scrape, crawl, interact with, monitor, download, or parse web and document content through the Firecrawl CLI."
policy:
  allow_implicit_invocation: true
