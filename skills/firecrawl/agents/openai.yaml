interface:
  display_name: "Firecrawl"
  short_description: "Use Firecrawl CLI for cache-aware web extraction"
  default_prompt: "Use $firecrawl to reuse local .firecrawl artifacts, then search, scrape, map, crawl, interact with, monitor, research, download, diagnose, give feedback on, or parse web and document content through the Firecrawl CLI."
policy:
  allow_implicit_invocation: true
